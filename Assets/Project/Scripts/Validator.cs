using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Validator
{

    public bool IsLevelValid(string fullWordToCompleteLevel, string currentClustersWord)
    {
        if (fullWordToCompleteLevel == currentClustersWord)
        {
            return true;
        }
        else
        {
            return true;
        }
    }
}
